import './App.css';
import Theme from './components/Theme';
import SiteContainer from './containers/SiteContainer';

function App() {
  return (
    <Theme>
      <SiteContainer />
    </Theme>
  );
}

export default App;
